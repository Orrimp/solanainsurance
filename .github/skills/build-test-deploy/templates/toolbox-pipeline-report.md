# Toolbox Pipeline Report

> **Generated:** {{TIMESTAMP}}
> **Command:** `solana-toolbox pipeline {{FLAGS}}`
> **Duration:** {{DURATION}}

---

## Pipeline Summary

| Step | Status | Details |
|------|--------|---------|
| cargo check | {{CHECK_STATUS}} | {{CHECK_DETAILS}} |
| cargo build-sbf | {{BUILD_STATUS}} | {{BUILD_DETAILS}} |
| cargo test | {{TEST_STATUS}} | {{TEST_DETAILS}} |
| solana deploy | {{DEPLOY_STATUS}} | {{DEPLOY_DETAILS}} |
| example client | {{CLIENT_STATUS}} | {{CLIENT_DETAILS}} |

**Legend:** ✅ PASS &nbsp;&nbsp; ❌ FAIL &nbsp;&nbsp; ⊝ SKIP

---

## Output

<details>
<summary>Full pipeline output (click to expand)</summary>

```
{{FULL_OUTPUT}}
```

</details>

---

## Key Metrics

{{#if BUILD_STATUS_PASS}}
- **Program binary:** `target/deploy/<program>.so` ({{BINARY_SIZE}} KB)
{{/if}}

{{#if TEST_STATUS_PASS}}
- **Tests passed:** {{TESTS_PASSED}}
{{/if}}

{{#if DEPLOY_STATUS_PASS}}
- **Program ID:** `{{PROGRAM_ID}}`
- **RPC URL:** `{{RPC_URL}}`
{{/if}}

---

## Result

{{#if ALL_PASSED}}
✅ **All pipeline steps passed**

The program is ready for use:
- Built and compiled to BPF
- All tests passing
{{#if DEPLOY_STATUS_PASS}}
- Deployed to local validator at {{RPC_URL}}
{{/if}}

### Next Steps
1. Run client: `cargo run --bin client`
2. Check logs: `solana logs`
3. Stop validator: Press Ctrl+C in validator terminal

{{else}}
❌ **Pipeline failed**

{{FAILURE_REASON}}

### Recovery Steps
1. Review error messages above
2. Fix the identified issue
3. Re-run: `solana-toolbox pipeline {{FLAGS}}`

{{/if}}

---

*Generated by the `build-test-deploy` skill using Solana Toolbox*
